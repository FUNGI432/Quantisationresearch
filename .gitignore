.venv/
__pycache__/
*.pyc

# Regenerable via prepare_data.py -- not committed (large, deterministic from code)
data/

# Trained model weights -- multi-GB each, regenerable via train_qat.py / run_matrix.py
checkpoints/

logs/

# Auto-regenerated every ~10 min while the pipeline runs (src/generate_live_status.py) --
# pure derived view of results/*.json, would just be commit noise.
docs/LIVE_STATUS.md
