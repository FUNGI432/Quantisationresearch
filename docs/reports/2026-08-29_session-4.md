# Session Report — 2026-08-29 (Day 4 of the v2 expansion)

## 0. Context

Continuation from [`2026-08-26_session-3.md`](2026-08-26_session-3.md). At
the start of this session: OPT-350M and Pythia-410M fully complete,
Qwen2.5-0.5B's baselines done, and its `none`/seed=42 QAT run paused mid-flight
at step 200/500 (checkpointed via the mid-training checkpointing built on
Day 3). This session resumed that run, watched it progress, then deliberately
paused again to take stock of the project's actual state rather than keep
running on autopilot.

## 1. Resume verification (second real-world use)

Resuming `run_matrix.py --model qwen2.5-0.5b` correctly picked up
`none`/seed=42 from step 200 (not step 0) across a multi-day gap (paused
2026-08-26, resumed 2026-08-28) -- confirmed by reading the checkpoint's
`step` field directly rather than trusting stdout (which was buffered and
didn't show the "Resuming from step" confirmation line in real time, a
known quirk of this environment). This is the second successful real-world
use of the checkpointing built on Day 3, now also proven to survive a
multi-day gap between stop and resume, not just a same-session pause.

Progress observed over this session: step 200 -> 300 -> 350 (of 500),
cumulative elapsed training time 5,776s -> 14,253s -> 18,445s (~5.1 hours).
The run was still going when deliberately stopped again at the end of this
session (see §4) -- **the same unresolved slowdown pattern from Day 3
continued**, now confirmed to persist across a session boundary too, not
just within one sitting. A single "control" run (the simplest, cheapest
config -- no FakeQuant overhead at all) taking 5+ hours and still not done
is a significant escalation from the ~20-45 minutes this exact configuration
took on OPT-350M and Pythia-410M.

## 2. Direct assessment requested: time estimate, methodology soundness, gaps

Asked directly for an honest read on three things. Answering honestly here
rather than reassuringly:

### Time estimate

At the observed pace (350/500 steps in 5.1 hours), finishing just this one
run would take **~7-8 hours total**. Whether the other 11 Qwen runs behave
the same way is unknown -- Pythia's slowdown (Day 2/3) was *intermittent*,
not universal, so there's real uncertainty in both directions:
- **Best case**: this is an anomaly specific to this run/session, and
  subsequent runs return to the ~20-45 minute norm. Remaining matrix:
  perhaps 6-10 more hours.
- **Worst case**: the slowdown is now the norm for Qwen specifically.
  Remaining matrix: potentially 2-3+ days.

This is a genuine unknown, not a hedge -- the pipeline's performance
characteristics on this specific model are not understood well enough to
forecast confidently, which is itself worth stating plainly rather than
picking a number that sounds authoritative.

### Is the methodology sound?

Yes, on the core experimental design:
- Multi-model architectural diversity (OPT/Pythia/Qwen) directly answers
  the "does this generalize" reviewer critique, and has already produced a
  genuinely interesting, non-obvious result (activation-quantization
  sensitivity varies by ~30x between OPT and Pythia).
- The FP16 fine-tuned control correctly isolates domain adaptation from the
  quantization effect -- this was a real methodological fix from early in
  the project (v1's original mistake), and it's held up under scrutiny.
- 3-seed variance is real, measured data now, not a single anecdote.
- The memory-scaling law (docs/MATH.md) is empirically fitted and validated
  against 2 real models, not asserted from a rough estimate.

### What's actually still lacking -- not hedged, a real list

| Gap | Status |
|---|---|
| SmoothQuant baseline | Broken on 2 of 3 models (Pythia, Qwen); OPT's number is the only trustworthy one. Root cause still unknown as of this session. |
| AWQ baseline | Missing entirely -- no Windows/triton wheel compatible with this setup. Reviewer asked for this by name. |
| QLoRA / memory-frontier experiment (Section B) | **Not started at all.** This is the piece that most directly delivers on "test to the absolute limit of the laptop" and the reviewer's QLoRA-comparison request. |
| Downstream eval (Section C) | Only ever run as a 10-example smoke test (`lm-eval-harness`, LAMBADA). Zero real per-checkpoint downstream numbers exist. |
| Real significance test (Section D) | Still a caveated placeholder Welch's t-test on 3 aggregate points. The real paired Wilcoxon test needs per-example NLL logging that was never added to the eval loop. |
| Unexplained slowdown anomaly | Beyond wall-clock cost, this is a credibility risk: a "slow" run's *results* haven't been separately sanity-checked against "fast" runs of the same config to rule out the slowness masking a correctness issue rather than just a performance one. Not yet done. |
| Societal impact section | Not drafted (reviewer critique #6) -- a writing task, not experimental, but still fully open. |

Net: **3 of the 6 original ACL reviewer critiques (QLoRA comparison,
downstream eval, real significance testing) remain completely unaddressed**,
not partially done. The 2-of-3-models-complete matrix is real progress, but
it answers only 1 of the 6 critiques (architectural diversity) fully.

## 3. Improvement points identified this session (not yet acted on)

1. **Sanity-check slow-run correctness, not just speed.** Before trusting
   any perplexity number from an anomalously slow run, verify it against an
   independent quick recomputation (e.g. re-evaluate the resulting
   checkpoint's perplexity on a small subset immediately) to rule out the
   slowdown being a symptom of something computing incorrectly rather than
   just computing slowly.
2. **Decide on parallelization of remaining work rather than strict
   sequencing.** Given Qwen's matrix alone may take days, it may be more
   productive to start Section B (QLoRA frontier) and add per-example NLL
   logging for Section D now, in parallel with -- rather than strictly
   after -- Qwen's matrix finishing. This wasn't decided this session; it's
   flagged as a real choice to make, not a default to fall into.
3. **Investigate whether the slowdown correlates with anything
   measurable** (time of day / other processes on the laptop / thermal
   state) rather than continuing to treat it as unexplained noise -- no
   data has been gathered specifically aimed at this yet, only anecdotal
   GPU-utilization checks that rule out a hang but don't explain the slowness.

## 4. Deliberate stop

Stopped `none`/seed=42 at step 350/500 (5.12 hours of training preserved)
via the checkpointing verified on Day 3 -- confirmed GPU fully released
(20% utilization, 497 MiB baseline) and the checkpoint intact before ending
the session. This stop was a deliberate decision to take stock of the
project's actual state (§2) rather than a reaction to a problem.

## 5. Continuation, same day: resumed Qwen + started closing the Section D gap

Resumed `none`/seed=42 again (picked the "keep grinding Qwen" half of the
§3.2 decision, while simultaneously acting on the "add per-example NLL
logging" half in parallel -- both improvement points from earlier today,
rather than picking one and dropping the other). This is genuine
progress against the improvement points identified in §3, on the same day
they were written down.

**Section D (the fully-unaddressed real significance test) now has its
missing piece built:**

- `common.evaluate_perplexity(..., return_per_example=True)` computes one
  mean-NLL-per-example value for every example in an eval pass, via manual
  `reduction='none'` cross-entropy (the model's built-in `labels=`-based
  loss only returns a single batch-mean scalar, which loses per-example
  information the moment batch_size > 1). Requires an unshuffled dataloader
  so example index `i` refers to the same underlying example across
  different runs -- true of every eval_loader in this codebase already.
- Saved to a compact `.npy` file per (model, config, seed) rather than
  inlined into results.json, referenced via a new `per_example_nll_path`
  field.
- `stats.py` rewritten to actually run the real paired Wilcoxon
  signed-rank test PLAN.md always described, per seed (pairing the same
  held-out example's NLL under two configs), falling back to the old
  caveated t-test only when per-example data isn't available for a given
  run. This is implemented, not yet exercised on real multi-seed data --
  it activates automatically once a real run finishes with the new logging.
- A backfill script (`backfill_per_example_nll.py`) can retroactively add
  this to all 24 already-completed OPT-350M/Pythia-410M runs by
  re-evaluating their saved checkpoints (no retraining) -- verified with
  `--dry-run` (correctly identified all 12 of OPT-350M's runs as needing
  it, touched zero GPU memory). **Not yet actually run** -- it needs
  ~2-4GB of VRAM headroom per checkpoint, and Qwen's training run is
  currently using the GPU; deliberately deferred rather than pausing Qwen
  again just to run it.

All of this was written and syntax-verified (`py_compile`) while Qwen
trained in the background, without touching the GPU or slowing it down --
confirmed by checking GPU utilization stayed at 100% throughout.

## 6. Status / what's left

- OPT-350M, Pythia-410M: fully complete. Their per-example NLL backfill is
  ready to run but not yet executed (§5) -- needs a GPU-idle window.
- Qwen2.5-0.5B: baselines done; `none`/seed=42 resumed and training again
  past step 350/500 as of this report; 11 runs untouched.
- Section D (real significance testing): infrastructure now built and
  verified syntactically, not yet exercised on real data. This moves it
  from "fully unstarted" to "built, awaiting a GPU-idle window to
  backfill + a completed run to test on" -- genuine but partial progress
  on one of the three critiques flagged as fully unaddressed in §2.
- Sections B (QLoRA frontier) and C (downstream eval) remain fully
  unstarted -- still the two biggest open items.
- The unexplained slowdown (§1, §3) remains unresolved and is still
  ongoing on this same `none`/seed=42 run as of this report.
