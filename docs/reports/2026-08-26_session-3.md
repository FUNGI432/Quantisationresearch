# Session Report — 2026-08-26 (Day 3 of the v2 expansion)

## 0. Context

Continuation from [`2026-08-24_session-2.md`](2026-08-24_session-2.md). At the
start of this session: OPT-350M and Pythia-410M both fully complete (24/36
total QAT runs), Qwen2.5-0.5B untouched. Two things prompted this session's
work before resuming the matrix:

1. A direct question about whether the statistical test referenced in
   `docs/PLAN.md` (Wilcoxon signed-rank) was actually implemented. Answer:
   no -- only a placeholder Welch's t-test on 3 aggregate points exists in
   `src/stats.py`, explicitly caveated as not a real substitute. Documented,
   not yet fixed (needs per-example NLL logging first, which isn't wired up).
2. A direct question about whether "record the full training setup and
   hyperparameters" was actually being done. Answer: partially -- this
   surfaced a real gap (§1 below).

## 1. Found and fixed: a documentation/code mismatch in reproducibility logging

`docs/PLAN.md` Section F explicitly claimed "every run writes full
hyperparameters + seed + git commit hash into its results.json entry." This
was not true. Per-run records had strategy/seed/steps/batch size/grad-accum
steps/learning rate, but were missing: the git commit hash, dataset
name/config, max sequence length, tokenization/eval sample counts, the
optimizer name, the gradient-checkpointing flag, and the exact FakeQuant
quantization scheme (per-channel vs per-tensor, observer type).

Fixed by adding `common.run_metadata()` (with a best-effort
`git_commit_hash()` that degrades to `"unknown"` rather than ever failing a
training run) and wiring it into every result-logging path: `train_qat.py`,
`eval_baseline.py`, `smoothquant_manual.py`. Verified end-to-end with a live
3-step smoke test before trusting it for real runs (see commit `e45a086`).

**This does not require re-running any of the 24 already-completed runs** --
every run in this project used identical hyperparameters from the single
source of truth (`config.py`), which is itself version-controlled, so the
actual experimental settings for old runs remain fully recoverable from git
history even without being stamped inline on each result.

## 2. Found and fixed: a real internal inconsistency in OPT-350M's eval sets (this one DID need action, but not retraining)

While answering the "would old runs need to be re-run" question, checked the
eval-side data directly and found that OPT-350M's own results table was not
internally consistent:

| Configuration | Eval set actually used |
|---|---|
| Control (all 3 seeds) | Full 10,000-example set (ran before the Day 2 eval-speed fix) |
| Weights-only, seed 42 & 1337 | Full 10,000-example set (ditto -- these were the real re-runs after the resumability bug fix, but still before the eval-speed fix landed) |
| Weights-only, seed 2024 | 1,500-example subset (ran right after the eval-speed fix) |
| Activations-only, both (all seeds) | 1,500-example subset |

Pythia-410M was unaffected -- it was trained entirely after the eval-speed
fix landed, so all 12 of its runs consistently used the 1,500-example
subset. This OPT-350M inconsistency happened because the fix landed mid-way
through that model's matrix, and the original resume logic had no way to
know a completed run's *eval set* had changed underneath it, only that a
result existed for that (strategy, seed) pair.

**Fix: `src/reeval_subset_fix.py`, re-evaluation only, no retraining.** Every
run already had a saved FP16 checkpoint. Loaded the 5 affected checkpoints
(`load_trained_checkpoint()`, reused from `eval_downstream.py`) and re-ran
`evaluate_perplexity()` on the same 1,500-example subset used everywhere
else, replacing only the eval-side fields (perplexity, eval time, peak eval
VRAM, token count) while preserving the original training-side fields
(training time, peak training VRAM, quantization error logs) unchanged,
since those were never affected by which eval set was used.

Result: numbers shifted slightly (control mean 21.12 -> 21.02, weights-only
mean 21.18 -> 21.09) but **the qualitative finding is unchanged** -- the
relative gap between weights-only and the control is essentially identical
before and after (~0.3% either way). This is worth stating plainly: the fix
mattered for methodological rigor and for withstanding review scrutiny, not
because the original numbers were meaningfully wrong.

### Updated OPT-350M table (fully eval-set-consistent now)

| Configuration | Perplexity |
|---|---|
| FP16 zero-shot | 41.28 |
| INT8 PTQ | 41.39 |
| SmoothQuant PTQ | 43.67 |
| FP16 fine-tuned control | **21.02** |
| QAT weights-only | 21.09 |
| QAT activations-only | 21.84 |
| QAT both | 21.92 |

## 3. Infrastructure incident: HuggingFace Hub connectivity failure

First attempt to launch the Qwen2.5-0.5B pipeline failed within seconds --
every HTTP request to `huggingface.co` returned `[Errno 11002] getaddrinfo
failed` (DNS resolution failure), meaning no network connectivity at that
moment. This is notable because it's the *first* time this project has
needed to download Qwen2.5-0.5B's actual model weights (earlier sessions
only cached its tokenizer via `prepare_data.py`), so there was no local
cache to fall back on -- the failure was immediate and total rather than a
partial/degraded run.

Diagnosed with a direct `ping`/`curl` check against `huggingface.co`, which
confirmed connectivity had returned (this was a transient network blip, not
a persistent outage or a firewall/DNS misconfiguration). Retried the
identical command and it proceeded normally. No code changes were needed --
this is logged here because a background pipeline that fails silently and
"completes" in seconds looks identical, from the outside, to one that
finished a full run successfully, which is worth being able to distinguish
at a glance later.

## 4. Qwen2.5-0.5B baselines complete; an unexplained training-phase slowdown, then a deliberate stop

After the retry in §3, Qwen2.5-0.5B's PTQ baselines completed cleanly:
FP16 zero-shot 21.61 PPL, INT8 dynamic 21.84 PPL, SmoothQuant (manual) 88.17
PPL. That SmoothQuant number is a second data point for a pattern first seen
on Pythia-410M (§ session-2, and the known-issues list): **2 of 3 models now
show large SmoothQuant degradation** (Pythia +768%, Qwen +308%), while
OPT-350M is fine (+5.8%). Notably, Qwen uses separate q/k/v/o projections
(not Pythia's fused QKV), so the "fused QKV is the cause" hypothesis from
Day 2 does not explain Qwen's failure -- something more general in
`smoothquant_manual.py` (calibration sample count, or `alpha=0.5` not being
well-tuned across architectures) is a more likely culprit now. Still not
root-caused; flagged more prominently given it's no longer a single-model
anomaly.

The QAT matrix started immediately after (`none`/seed=42, the control run).
GPU sat at 99-100% utilization throughout -- genuinely computing, not
stalled -- but training alone ran for **over 2.5 hours** without completing
500 steps, versus OPT-350M and Pythia-410M's typical 20-45 minutes for the
same config. This is very likely the same unexplained-slowdown phenomenon
documented in `2026-08-24_session-2.md` §2.2 (there, it hit *eval* time on
Pythia by up to ~40x; here it appears to be hitting *training* time on
Qwen), now confirmed to not be strategy-specific, model-specific, or
eval-vs-train-phase-specific -- it has now shown up in three different
places across two different models. This is a real, still-unresolved
infrastructure problem, not three unrelated coincidences.

At the user's request, the run was killed directly (not via the usual
"wait for completion, then stop" pattern used on Days 1-2) given how far
past every expectation it had already run, with explicit acceptance that
this loses that run's progress -- **the pipeline has no mid-training
checkpointing**, only a checkpoint saved after all 500 steps + eval
complete. Verified after stopping: GPU VRAM fully released (327 MiB, no
training process), `results/qwen2.5-0.5b.json` contains only the 4 baseline
entries and no partial/corrupt QAT entries (the killed run never reached its
`append_result` call, so there was nothing to clean up this time).

**Recommended for next session, not yet implemented:** add mid-training
checkpointing (e.g., every 100 steps) and resume-from-partial-checkpoint
logic to `train_qat.py`. Given how unpredictable run duration has turned out
to be, losing 2.5+ hours of progress to a single stop decision is a real
cost this pipeline shouldn't keep paying.

## 5. Built and verified: mid-training checkpointing, in direct response to §4

The recommendation at the end of §4 was acted on the same day, prompted by a
direct question ("would this affect the quality of the AI?") that needed a
real, considered answer before shipping the feature, not just an assumption.

**Quality answer (given before implementing, so it could shape the design,
not justify it after the fact):** no meaningful quality impact, *provided*
the checkpoint captures (a) the full model state including FakeQuantize
observer buffers, not just trainable parameters, (b) the optimizer state
(AdamW8bit momentum/variance -- skipping this would reset momentum on
resume, a small but avoidable optimization discontinuity), and (c) the exact
step count. The only thing that can't be perfectly restored is the exact
data-shuffling order at the pause point -- but that's a strictly smaller
source of variation than the seed variance this whole study already
measures and accounts for, so it doesn't bias the science.

**Implementation** (`train_qat.py`, `config.CHECKPOINT_EVERY_STEPS=50`):
- Periodic save every 50 steps (10% of a run) to a separate
  `RESUME_<model>_<strategy>_<seed>.pt` file (distinct from the final
  FP16 eval-ready checkpoint) -- full FP32 model state_dict, full optimizer
  state_dict, current step, and cumulative elapsed training time (so
  `train_time_s` in the final result is correct across a resume, not reset
  to only the last segment). Written atomically (`os.replace` after writing
  to a `.tmp` file) so a kill mid-write can never leave a corrupt checkpoint.
- SIGINT/SIGTERM handler saves an immediate checkpoint before re-raising, for
  a graceful interactive Ctrl+C.
- On real completion, the resume file is deleted (only needed mid-flight).
- Resume logic at the top of `train_one()`: if a resume file exists for this
  exact (model, strategy, seed), load it and continue from that step instead
  of starting over; the final result records `resumed_from_checkpoint` so
  this is visible in the data, not silently hidden.

**Verified, not just written:**
1. Normal run to completion (steps=6, checkpoint every 2) -- periodic
   checkpoints appeared and were correctly deleted after completion.
2. **Realistic scenario**: started a longer run (steps=20), let one periodic
   checkpoint land at step 4, then hard-killed the process with `TaskStop`
   (a forceful terminate, not a graceful signal -- this matters, because the
   SIGTERM handler likely would NOT catch a native-Windows `TaskStop`-style
   kill; the periodic checkpoint is what actually provides the safety net in
   that case, not the signal handler). Confirmed GPU VRAM fully released
   (345 MiB, baseline) and the checkpoint file intact (step=4, populated
   optimizer state, correct FakeQuantLinear-wrapped model keys).
3. Resumed the same (model, strategy, seed): correctly picked up at step 4
   (not 0), completed the remaining 16 steps, `train_time_s` correctly
   reflected cumulative time across both segments (176.3s = ~24.3s before
   the kill + ~152s after resuming), `resumed_from_checkpoint=True` recorded
   in the result, and the resume file was cleaned up on completion.

**Caveat worth keeping in mind**: the SIGTERM/SIGINT immediate-save path is
untested against a real external kill on this Windows setup (Python's
SIGTERM handling on native Windows does not reliably intercept an external
`taskkill`/`TerminateProcess`-style termination the way POSIX SIGTERM does).
The periodic 50-step checkpoint is therefore the mechanism actually being
relied on in practice, and it is the one verified above end-to-end. A
graceful in-terminal Ctrl+C should still trigger the signal handler
correctly, but that specific path hasn't been separately confirmed.

## 6. Status / what's left

- OPT-350M: fully complete, now fully eval-set-consistent.
- Pythia-410M: fully complete, was already eval-set-consistent.
- Qwen2.5-0.5B: PTQ baselines complete (FP16 21.61, INT8 21.84, SmoothQuant
  88.17 -- see §4). QAT matrix: 0/12 -- the one attempted run was killed
  deliberately per §4, no partial results saved.
- Reproducibility logging gap (§1) closed for all runs going forward.
- Mid-training checkpointing implemented and verified (§5) -- future runs
  can be safely paused (even via a hard kill) and resumed without losing
  more than ~50 steps of progress, addressing the cost identified in §4.
- SmoothQuant is now confirmed broken on 2 of 3 models (Pythia, Qwen), not
  just Pythia -- needs a real fix, not just a Pythia-specific patch (§4).
- The training/eval-time slowdown anomaly (first seen Day 2 on Pythia's
  eval, now seen on Qwen's training) is confirmed to recur across models and
  phases -- treat as a real unresolved infrastructure issue, not noise. This
  is now less costly to work around thanks to §5, but still not understood.
- Still not started: the memory-frontier/QLoRA experiment (Section B),
  downstream zero-shot evaluation (Section C), the real Wilcoxon
  significance test (Section D -- needs per-example NLL logging added
  first).
